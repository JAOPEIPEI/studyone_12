<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#008080</item>
        <!--radiobutton color-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="dialog" parent="@android:style/Theme.Dialog">//dialog
        <item name="android:textSize">35sp</item>
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">#880000</item>
        <item name="android:textColorPrimary">#433ABC</item>
        <item name="android:windowNoTitle">true</item>

    </style>

    <style name="NavigationViewStyle">//drawer
        <item name="android:textSize">30sp</item> <!-- menu item text size-->
        <item name="android:listPreferredItemHeightSmall">70dp</item><!-- menu item height-->
    </style>
</resources>
